<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('partials/head') %>
</head>
<body>
  <%- include('partials/navbar') %>
  
  <div class="container text-center mt-5">
    <div class="p-5 mb-4 bg-light rounded-3 shadow-sm">
      <div class="container-fluid py-5">
        <h1 class="display-5 fw-bold">Bienvenido a <%= title %></h1>
        <p class="col-md-8 fs-4 mx-auto">Gestiona tus productos, revisa el stock y administra tu negocio con Northwind App.</p>
        <div class="d-flex justify-content-center gap-3">
            <a href="/catalogo" class="btn btn-primary btn-lg" type="button">Ver Catálogo</a>
            <a href="/nuevo-producto" class="btn btn-outline-secondary btn-lg" type="button">Agregar Producto</a>
        </div>
      </div>
    </div>
    
    <% if (user) { %>
      <div class="alert alert-info d-inline-block">
        Hola, <strong><%= user.name %></strong> (<%= user.email %>)
      </div>
    <% } %>
  </div>
</body>
</html>